<li class="media thumbnails">
  <%= link_to image_tag(comment.user.profile.face.thumb.url), comment.user, class: "pull-left span2" %>
  <div class="media-body">
    <h4 class="media-heading"><%= comment.user.name %></h4>
    <p><%= comment.content %></p>
  </div>
</li>