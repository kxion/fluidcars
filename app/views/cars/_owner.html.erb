<div class="border-box">
  <div class="media">
    <%= link_to image_tag(owner.profile.avatar.url("100x100")), profile_path(owner), class: 'pull-left' %>
    <div class="media-body">
    <h4 class="media-heading"><%= owner.profile.name %></h4>
    <%= owner.profile.contact %>
    </div>
  </div>
</div>

